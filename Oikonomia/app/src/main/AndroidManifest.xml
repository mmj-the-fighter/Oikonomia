<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


<application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Oikonomia"
        tools:targetApi="31">

        <activity
            android:name=".OikonomiaActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".TransactionsViewerActivity"
            android:exported="true"
            android:label="TransactionsViewerActivity">
            <intent-filter>
                <action android:name="com.example.TransactionsViewerActivity" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".TransactionEditActivity"
            android:exported="true"
            android:label="TransactionEditActivity">
            <intent-filter>
                <action android:name="com.example.TransactionEditActivity" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".TransactionsSearchActivity"
            android:exported="true"
            android:label="TransactionsSearchActivity">
            <intent-filter>
                <action android:name="com.example.TransactionsSearchActivity" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

    </application>

</manifest>